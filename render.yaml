services:
  - type: web
    name: jules-discord-bot
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: JULES_API_KEY
        sync: false
